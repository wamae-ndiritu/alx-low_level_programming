#include <stdio.h>
#include "main.h"
/**
 * add - add two numbes
 * @num1: first number to be passed
 * @num2: second number
 *
 * Return: returns the sum
 */
int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return (sum);
}
