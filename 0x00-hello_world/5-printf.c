#include <stdio.h>
/**
 * main - main entry point of the program
 * @void - Takes no arguments
 *
 * Description - prints 'with proper grammar, but the outcome is a piece of art,' to the screen
 * Return - return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
