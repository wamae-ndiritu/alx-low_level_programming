#include <stdio.h>
#include "main.h"
/**
 * mul- takes two integers and returns their product
 *
 * @a: the first integer
 * @b: the second integer
 *
 * Return: returns the product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
