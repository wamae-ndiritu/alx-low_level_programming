#include <stdlib.h>
#include "main.h"
/**
 * _abs - gets the absolute value
 *
 * @n: number to get its absolute value
 *
 * Return: returns the absolute value
 */
int _abs(int n)
{
	int abs_value = abs(n);

	return (abs_value);

}
